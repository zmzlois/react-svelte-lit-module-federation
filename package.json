{
  "name": "react-svelte-lit-module-federation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pnpm run build && pnpm run dev",
    "build": "time pnpm --filter rspack-mf_* --parallel build",
    "serve": "pnpm --filter rspack-mf_* --parallel serve",
    "dev": "pnpm --filter rspack-mf_* --parallel dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rsdoctor/rspack-plugin": "^0.3.2",
    "@rsdoctor/webpack-plugin": "^0.3.2",
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}